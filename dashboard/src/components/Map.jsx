import React from 'react';

const Map = () => {
    return (
        <div style={{ width: '100%', height: '100%', backgroundColor: '#e0e0e0', display: 'flex', alignItems: 'center', justifyContent: 'center' }}>
            <h3>failed to fetch pose.</h3>
        </div>
    );
};

export default Map;
